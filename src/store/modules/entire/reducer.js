const initialState = {
    entire: '222'
}

function reducer(state = initialState, action) {
    switch (action.type) {
        case 'xxx':

            break;

        default:
            return state
    }
}

export default reducer